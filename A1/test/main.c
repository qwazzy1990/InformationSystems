#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "BTree.h"
#include "utilities.h"


int main(int argc, char* argv[])
{

    // BTree t = newBTree(2);
    // addKey(t, 4);
    // printf("%d\n", t->root->keys[0]);
    return 0;
}